# ROSCon 2026: VLA Finetuning

Open `finetune_molmoact2_libero.ipynb`.

Environment checks:

```bash
/ryzers/test_torch.sh
/ryzers/test_molmoact2.sh
```

Models are cached under `~/.cache/huggingface`. Training outputs and checkpoints
are written under `~/outputs` and `~/checkpoints`.
